import styled from 'styled-components'
import { colors } from '../../styles'

export const Headerts = styled.header`
  background-color: ${colors.black};

  div {
    text-align: center;

    img {
      width: 150px;
      height: 40px;
      margin: 20px 26px;
    }
  }
`
